# Quick Start Guide - AI Mock Interviewer\n\n## Running the Application\n\n### Development Mode\n\n```bash\n# Start all services (backend + frontend)\nnpm run dev\n\n# Or start separately:\nnpm run dev:server    # Backend on http://localhost:5000\nnpm run dev:client    # Frontend on http://localhost:5173\n```\n\n### Docker Services\n\n```bash\n# Start MongoDB, Redis, Piston\ndocker-compose -f docker-compose.dev.yml up -d\n\n# List running services\ndocker-compose -f docker-compose.dev.yml ps\n\n# Stop services\ndocker-compose -f docker-compose.dev.yml down\n```\n\n## Testing\n\n```bash\n# Run all tests\nnpm test\n\n# Backend tests only\nnpm run test:server\n\n# Frontend tests only\nnpm run test:client\n\n# E2E API tests (requires backend running)\nnode tests/e2e-api-test.js\n```\n\n## Key URLs\n\n| Service | URL | Purpose |\n|---------|-----|----------|\n| Frontend | http://localhost:5173 | React app |\n| Backend API | http://localhost:5000/api/v1 | REST API |\n| API Health | http://localhost:5000/api/v1/health | Health check |\n| MongoDB | mongodb://localhost:27017 | Database |\n| Piston | http://localhost:2000 | Code execution |\n| Redis | localhost:6379 | Cache (optional) |\n\n## Test Credentials\n\nFor manual testing, any email/password works:  \n- Email: `test@example.com`\n- Password: `Test123!`\n\n## Common Issues\n\n### Port Already in Use\n```bash\n# Find and kill process on port 5000 (backend)\nlsof -ti:5000 | xargs kill -9\n\n# Find and kill process on port 5173/5174 (frontend)\nlsof -ti:5173 | xargs kill -9\n```\n\n### MongoDB Connection Failed\n```bash\n# Ensure Docker container is running\ndocker-compose -f docker-compose.dev.yml up -d ai-interview-mongo-dev\n\n# Check logs\ndocker logs ai-interview-mongo-dev\n```\n\n### Piston Service Unavailable\n```bash\n# Restart Piston container\ndocker-compose -f docker-compose.dev.yml restart ai-interview-piston-dev\n\n# Check if running\ncurl http://localhost:2000/api/v2/runtimes\n```\n\n## File Structure\n\n```\nai-mock-interviewer/\nâ”œâ”€â”€ server/                    # Node.js backend\nâ”‚   â”œâ”€â”€ src/\nâ”‚   â”‚   â”œâ”€â”€ controllers/       # Route handlers\nâ”‚   â”‚   â”œâ”€â”€ services/          # Business logic (AI, interview, execution)\nâ”‚   â”‚   â”œâ”€â”€ models/            # MongoDB schemas\nâ”‚   â”‚   â”œâ”€â”€ routes/            # API routes\nâ”‚   â”‚   â”œâ”€â”€ middlewares/       # Auth, validation, error handling\nâ”‚   â”‚   â”œâ”€â”€ config/            # Configuration\nâ”‚   â”‚   â”œâ”€â”€ utils/             # Helpers\nâ”‚   â”‚   â”œâ”€â”€ validations/       # Input validation\nâ”‚   â”‚   â”œâ”€â”€ app.js             # Express app\nâ”‚   â”‚   â””â”€â”€ index.js           # Entry point\nâ”‚   â”œâ”€â”€ tests/                 # Test files\nâ”‚   â””â”€â”€ package.json\nâ”œâ”€â”€ client/                    # React frontend\nâ”‚   â”œâ”€â”€ src/\nâ”‚   â”‚   â”œâ”€â”€ pages/             # Page components\nâ”‚   â”‚   â”œâ”€â”€ components/        # Reusable components\nâ”‚   â”‚   â”œâ”€â”€ store/             # Redux slices\nâ”‚   â”‚   â”œâ”€â”€ services/          # API service\nâ”‚   â”‚   â”œâ”€â”€ utils/             # Helpers\nâ”‚   â”‚   â”œâ”€â”€ App.jsx            # Root component\nâ”‚   â”‚   â”œâ”€â”€ main.jsx           # Entry point\nâ”‚   â”‚   â””â”€â”€ index.css          # Global styles\nâ”‚   â”œâ”€â”€ test/                  # Test files\nâ”‚   â”œâ”€â”€ vite.config.js         # Vite configuration\nâ”‚   â””â”€â”€ package.json\nâ”œâ”€â”€ tests/                     # E2E tests\nâ”œâ”€â”€ docker-compose.dev.yml     # Docker services config\nâ”œâ”€â”€ docker-compose.yml         # Production Docker config\nâ””â”€â”€ package.json               # Root workspace config\n```\n\n## Performance Tuning\n\n### Improve Code Execution Speed\n```bash\n# In server/.env\nPISTON_URL=http://localhost:2000  # Direct URL, not docker hostname\n```\n\n### Database Optimization\n```bash\n# Create indexes on frequently queried fields\n# Already configured in models\n```\n\n### Frontend Performance\n```bash\n# Build optimized version\nnpm run build --workspace=client\n\n# Preview production build\nnpm run preview --workspace=client\n```\n\n## API Examples\n\n### Start Interview Session\n```bash\ncurl -X POST http://localhost:5000/api/v1/interviews/sessions \\\n  -H \"Authorization: Bearer YOUR_TOKEN\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"language\": \"javascript\",\n    \"topics\": [\"arrays\", \"strings\"],\n    \"maxQuestions\": 2,\n    \"timeLimitMinutes\": 30\n  }'\n```\n\n### Submit Answer\n```bash\ncurl -X POST http://localhost:5000/api/v1/interviews/sessions/SESSION_ID/submit \\\n  -H \"Authorization: Bearer YOUR_TOKEN\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"questionId\": \"QUESTION_ID\",\n    \"code\": \"function twoSum(nums, target) { ... }\",\n    \"language\": \"javascript\",\n    \"explanation\": \"I used a hash map to...\",\n    \"timeSpentMs\": 120000\n  }'\n```\n\n### Get Analytics\n```bash\ncurl http://localhost:5000/api/v1/analytics/dashboard?period=30d \\\n  -H \"Authorization: Bearer YOUR_TOKEN\"\n```\n\n## Environment Variables\n\n### Server (.env)\n```env\nNODE_ENV=development\nPORT=5000\nMONGODB_URI=mongodb://localhost:27017/ai-mock-interviewer\nJWT_ACCESS_SECRET=dev-access-secret\nJWT_REFRESH_SECRET=dev-refresh-secret\nJWT_ACCESS_EXPIRATION=30m\nJWT_REFRESH_EXPIRATION=7d\nAI_PROVIDER=mock  # or 'groq', 'huggingface', 'ollama'\nAI_API_KEY=your-key-here  # if using real provider\nAI_MODEL=llama-3.3-70b-versatile\nPISTON_URL=http://localhost:2000\nLOG_LEVEL=debug\n```\n\n## Deployment\n\n### Docker Build\n```bash\n# Build images\ndocker-compose -f docker-compose.yml build\n\n# Deploy\ndocker-compose -f docker-compose.yml up -d\n```\n\n### Environment Variables for Production\n```env\nNODE_ENV=production\nPORT=80  # or your production port\nMONGODB_URI=<set-in-secret-manager>\nJWT_ACCESS_SECRET=<set-in-secret-manager>\nJWT_REFRESH_SECRET=<set-in-secret-manager>\nAI_PROVIDER=groq  # Use real AI provider\nAI_API_KEY=<set-in-secret-manager>\nCLIENT_URL=https://yourdomain.com\nPISTON_URL=https://piston.yourdomain.com\n```\n\n---\n\n**For detailed information, see PROJECT_ANALYSIS_AND_IMPROVEMENTS.md**\n